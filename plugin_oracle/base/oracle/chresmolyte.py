# import os
# import sys
# from plugin_oracle.util.ctrl.screen import Screen

class Chresmolyte:
    def __init__(self) -> None:
        # _ = pyautogui.alert('This is an alert box.')
        print('Yeet')
        # screenshot()
    
    # def _find_template(self, template) -> bool:
    #     # # Capture the current complete screen.
    #     # screenshot = pyautogui.screenshot()
    #     # scr_width, scr_height = screenshot.size
    #     # tmp_width, tmp_height = template.size

    #     # # Naively slide the template over the screenshot
    #     # for x in range(scr_width - tmp_width + 1):
    #     #     for y in range(scr_height - tmp_height + 1):
    #     #         # Crop out a region from the screenshot with same size as the template
    #     #         region = self.screenshot.crop((x, y, x + tmp_width, y + tmp_height))
    #     #         # Compare pixel data for an exact match
    #     #         if list(region.getdata()) == list(self.template.getdata()):
    #     #             return True
    #     # return False
    #     print('neet')
    #     return True